<!--<% @coupons.each do |c| %>
    <p>Coupon code: <%=link_to c.coupon_code, coupon_path(c) %> for: <%= c.store %> </p>
<%end%>-->

<% @coupons.each do |coupon| %>
  <div><%= link_to coupon.coupon_code, coupon_path(coupon) %> | <%= coupon.store %></div>
<% end %>